import { styled } from '@mui/material/styles';

export const SearhBarSection = styled('div')({
  maxWidth: `100%`,
  padding: '2rem 15px',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
});
