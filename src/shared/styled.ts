import { styled } from '@mui/material/styles';

export const SMainPageSection = styled('div')({
  maxWidth: `100%`,
  margin: '0 auto',
  padding: '1rem',
});

export const SubPageSection = styled('div')({
  margin: '0 auto',
  background: '#fff',
  borderRadius: 10,
  overflow: 'hidden',
  boxShadow:
    '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
});
